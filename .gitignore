# compiled output
/dist
# Leading slash anchors to the repo root, so the example app's build
# output needs its own rule.
examples/*/dist/
/node_modules

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
test-report.junit.xml
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# ENV
.env
# Astro docs site
docs/dist/
docs/.astro/
# API reference markdown is generated from JSDoc on every build.
docs/src/content/docs/api/
# Generated subpath stub for legacy Node resolution. Leading slash matters:
# a bare `terminus/` would also ignore lib/terminus/, the actual source.
/terminus/
